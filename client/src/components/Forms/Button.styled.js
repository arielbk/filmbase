import styled from 'styled-components';

const StyledButton = styled.button`
	padding: 0.5rem 1rem;
	border-radius: 3px;
	font-size: 0.9rem;

	:hover {
		cursor: pointer;
	}
`;

export default StyledButton;
