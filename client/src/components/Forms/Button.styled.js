import styled from 'styled-components';

const StyledButton = styled.button`
	padding: 1rem 2rem;
	border-radius: 3px;
	border: none;
	font-size: 0.9rem;
	background: #111;
	color: #eee;
	margin: 0 2rem;

	:hover {
		cursor: pointer;
	}
`;

export default StyledButton;
